# API URL (optional - defaults to empty string which uses the Vite proxy)
# For production, set this to your API server URL
VITE_API_URL=

# Example for production:
# VITE_API_URL=https://your-api-server.com
